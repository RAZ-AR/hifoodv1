# Настройка автокомплита адресов и зон доставки

Эта функция добавляет автокомплит адресов через Google Maps API и проверку зон доставки для приложения Hi Food.

## 📋 Оглавление

1. [Что было добавлено](#что-было-добавлено)
2. [Настройка Google Maps API](#настройка-google-maps-api)
3. [Настройка базы данных](#настройка-базы-данных)
4. [Настройка зон доставки](#настройка-зон-доставки)
5. [Переменные окружения](#переменные-окружения)
6. [Тестирование](#тестирование)

---

## 🎯 Что было добавлено

### Backend
- ✅ Google Maps Services интеграция
- ✅ PostGIS расширение для работы с геоданными
- ✅ Таблица `delivery_zones` с полигонами
- ✅ API endpoints:
  - `GET /api/geocoding/autocomplete` - автокомплит адресов
  - `GET /api/geocoding/place/:placeId` - детали места
  - `POST /api/geocoding/geocode` - геокодирование адреса
  - `POST /api/delivery-zones/check` - проверка зоны доставки
  - `GET /api/delivery-zones` - получение всех зон

### Frontend
- ✅ Компонент `AddressAutocomplete` с автокомплитом
- ✅ Визуальная индикация зоны доставки
- ✅ Интеграция в `CheckoutForm`
- ✅ Валидация адресов в зоне доставки
- ✅ Многоязычная поддержка (EN, SR, RU)

### Database
- ✅ Таблица `delivery_zones` с PostGIS
- ✅ Поля для координат в `orders`
- ✅ Функция `check_address_in_delivery_zone()`
- ✅ Примерные зоны для Белграда

---

## 🗺️ Настройка Google Maps API

### Шаг 1: Создайте проект в Google Cloud

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в **APIs & Services** → **Library**

### Шаг 2: Включите необходимые API

Включите следующие API:

- ✅ **Places API** (для автокомплита)
- ✅ **Geocoding API** (для преобразования адресов в координаты)
- ✅ **Maps JavaScript API** (опционально, для отображения карт)

### Шаг 3: Создайте API ключ

1. Перейдите в **APIs & Services** → **Credentials**
2. Нажмите **Create Credentials** → **API key**
3. Скопируйте созданный ключ

### Шаг 4: Ограничьте API ключ (рекомендуется)

#### Для бэкенда:
- **Application restrictions**: None (или IP addresses)
- **API restrictions**: Restrict key
  - Places API
  - Geocoding API

#### Для фронтенда (если используете):
- **Application restrictions**: HTTP referrers
  - `https://raz-ar.github.io/*`
  - `http://localhost:5173/*` (для разработки)
- **API restrictions**: Restrict key
  - Maps JavaScript API

### Шаг 5: Мониторинг использования

Google Maps предоставляет **$200 бесплатно каждый месяц**:
- Автокомплит: $2.83 за 1000 запросов
- Геокодирование: $5.00 за 1000 запросов

Примерно **70,000 автокомплитов** или **40,000 геокодирований** бесплатно в месяц.

---

## 🗄️ Настройка базы данных

### Шаг 1: Запустите миграцию

В Supabase SQL Editor выполните миграцию:

```bash
# Откройте файл миграции
/home/user/hifoodv1/backend/migrations/001_add_delivery_zones.sql
```

Или выполните вручную в Supabase Dashboard → SQL Editor.

### Шаг 2: Проверьте таблицу

```sql
SELECT * FROM delivery_zones;
```

Вы должны увидеть 3 примерные зоны для Белграда:
- Центр города
- Нови Београд
- Земун

### Шаг 3: Проверьте функцию

```sql
SELECT * FROM check_address_in_delivery_zone(44.8125, 20.4612);
```

Должен вернуть информацию о зоне, если координаты внутри.

---

## 🎯 Настройка зон доставки

### Метод 1: Через Google My Maps (рекомендуется)

1. Откройте [Google My Maps](https://www.google.com/maps/d/u/0/)
2. Создайте новую карту
3. Используйте инструмент "Draw a line" → "Add line or shape"
4. Нарисуйте полигон зоны доставки
5. Кликните на полигон → "Export to KML/KMZ"
6. Извлеките координаты из файла

### Метод 2: Через geojson.io

1. Откройте [geojson.io](https://geojson.io)
2. Нарисуйте полигон на карте
3. Скопируйте координаты из JSON справа
4. Конвертируйте в формат PostGIS

### Пример добавления зоны:

```sql
INSERT INTO delivery_zones (
  name,
  description,
  boundary,
  delivery_fee,
  free_delivery_threshold,
  min_order_amount,
  estimated_delivery_time,
  priority,
  color
) VALUES (
  'Нови Београд',
  'Район Нови Београд',
  ST_GeogFromText('POLYGON((
    20.4000 44.8200,
    20.4400 44.8200,
    20.4400 44.7900,
    20.4000 44.7900,
    20.4000 44.8200
  ))'),
  300, -- 300 RSD за доставку
  2500, -- бесплатная доставка от 2500 RSD
  1000, -- минимальный заказ 1000 RSD
  35, -- 35 минут
  2, -- приоритет (чем выше, тем важнее)
  '#2196F3' -- цвет зоны на карте
);
```

### Обновление существующей зоны:

```sql
UPDATE delivery_zones
SET
  boundary = ST_GeogFromText('POLYGON((
    -- новые координаты
  ))'),
  delivery_fee = 250,
  free_delivery_threshold = 2000
WHERE name = 'Нови Београд';
```

---

## ⚙️ Переменные окружения

### Backend (.env)

```bash
# Google Maps API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Supabase (обязательно для зон доставки)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

# Остальные переменные...
TELEGRAM_BOT_TOKEN=your_bot_token
PORT=3000
NODE_ENV=production
```

### Frontend (.env)

```bash
# Google Maps API (опционально, если используете Maps на фронтенде)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# API Backend
# VITE_API_BASE_URL=http://localhost:3000 # для локальной разработки
```

---

## 🧪 Тестирование

### 1. Тест автокомплита

```bash
curl "https://hifoodv1.onrender.com/api/geocoding/autocomplete?input=Knez%20Mihailova"
```

Ожидаемый результат: массив с предложениями адресов.

### 2. Тест получения деталей места

```bash
curl "https://hifoodv1.onrender.com/api/geocoding/place/YOUR_PLACE_ID"
```

### 3. Тест проверки зоны доставки

```bash
curl -X POST https://hifoodv1.onrender.com/api/delivery-zones/check \
  -H "Content-Type: application/json" \
  -d '{"latitude": 44.8125, "longitude": 20.4612}'
```

Ожидаемый результат:
```json
{
  "in_zone": true,
  "delivery_fee": 200,
  "free_delivery_threshold": 2000,
  "min_order_amount": 800,
  "estimated_delivery_time": 25,
  "message": "Доставка в зону \"Центр города\" - 200 RSD"
}
```

### 4. Проверка во фронтенде

1. Откройте приложение
2. Добавьте товар в корзину
3. Перейдите к оформлению заказа
4. Начните вводить адрес в поле "Адрес доставки"
5. Выберите адрес из списка
6. Должна появиться информация о зоне доставки

---

## 🎨 Настройка цветов зон

Зоны имеют цветовую кодировку для визуализации на карте:

- 🟢 Зеленый (`#4CAF50`) - основная зона
- 🔵 Синий (`#2196F3`) - вторая зона
- 🟠 Оранжевый (`#FF9800`) - дальняя зона
- 🔴 Красный (`#F44336`) - очень дальняя зона

---

## 📊 Структура базы данных

### Таблица `delivery_zones`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `name` | TEXT | Название зоны |
| `description` | TEXT | Описание |
| `boundary` | GEOGRAPHY(POLYGON) | Границы зоны (PostGIS) |
| `delivery_fee` | DECIMAL | Стоимость доставки (RSD) |
| `free_delivery_threshold` | DECIMAL | Порог бесплатной доставки |
| `min_order_amount` | DECIMAL | Минимальная сумма заказа |
| `estimated_delivery_time` | INTEGER | Примерное время (минуты) |
| `is_active` | BOOLEAN | Активна ли зона |
| `priority` | INTEGER | Приоритет (для пересечений) |
| `color` | TEXT | Цвет для карты (hex) |

### Обновления в таблице `orders`

Добавлены поля:
- `delivery_coordinates` (GEOGRAPHY) - координаты адреса
- `delivery_zone_id` (UUID) - ссылка на зону
- `delivery_zone_name` (TEXT) - название зоны

---

## 🚨 Устранение неполадок

### Ошибка: "Сервис геокодирования недоступен"

**Причина**: Не установлен Google Maps API ключ или Supabase.

**Решение**:
```bash
# Проверьте переменные окружения
echo $GOOGLE_MAPS_API_KEY
echo $SUPABASE_URL

# Убедитесь, что они установлены в .env файле
```

### Ошибка: "Failed to autocomplete address"

**Причина**: Ограничения API ключа или превышен лимит.

**Решение**:
1. Проверьте [Google Cloud Console](https://console.cloud.google.com/)
2. Убедитесь, что Places API включен
3. Проверьте ограничения ключа
4. Проверьте квоты использования

### Ошибка: "Zone check error"

**Причина**: PostGIS функция не создана или нет зон в БД.

**Решение**:
```sql
-- Проверьте функцию
SELECT routine_name
FROM information_schema.routines
WHERE routine_name = 'check_address_in_delivery_zone';

-- Проверьте зоны
SELECT COUNT(*) FROM delivery_zones WHERE is_active = true;
```

---

## 📚 Дополнительные ресурсы

- [Google Maps Platform Documentation](https://developers.google.com/maps/documentation)
- [PostGIS Documentation](https://postgis.net/documentation/)
- [Supabase PostGIS Extension](https://supabase.com/docs/guides/database/extensions/postgis)
- [GeoJSON.io](https://geojson.io) - инструмент для создания полигонов

---

## 🎉 Готово!

Теперь ваше приложение поддерживает:
- ✅ Автокомплит адресов
- ✅ Проверку зон доставки
- ✅ Динамическую стоимость доставки
- ✅ Многоязычную поддержку
- ✅ Валидацию адресов

Для вопросов и поддержки обращайтесь к документации или создайте issue в репозитории.
