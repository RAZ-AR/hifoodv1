# 🗺️ Простой импорт зон доставки

Два простых способа настроить зоны доставки без программирования!

---

## 📝 Способ 1: JSON файл с адресами (САМЫЙ ПРОСТОЙ!)

### Шаг 1: Отредактируйте файл с адресами

Откройте файл `/backend/config/delivery-zones.json` и добавьте ваши адреса:

```json
{
  "zones": [
    {
      "name": "Центр города",
      "description": "Центральная часть Белграда",
      "delivery_fee": 200,
      "free_delivery_threshold": 2000,
      "min_order_amount": 800,
      "estimated_delivery_time": 25,
      "priority": 3,
      "color": "#4CAF50",
      "buffer_radius_km": 1.5,
      "addresses": [
        "Knez Mihailova, Belgrade",
        "Trg Republike, Belgrade",
        "Kalemegdan, Belgrade"
      ]
    }
  ],
  "default_buffer_radius_km": 1.5
}
```

**Параметры зоны:**

| Параметр | Обязательный | Описание | Пример |
|----------|-------------|----------|---------|
| `name` | ✅ Да | Название зоны | "Центр города" |
| `delivery_fee` | ✅ Да | Стоимость доставки (RSD) | 200 |
| `addresses` | ✅ Да | Список адресов в зоне | ["Knez Mihailova, Belgrade"] |
| `description` | ❌ Нет | Описание зоны | "Центральная часть" |
| `free_delivery_threshold` | ❌ Нет | Бесплатная доставка от (RSD) | 2000 |
| `min_order_amount` | ❌ Нет | Минимальный заказ (RSD) | 800 |
| `estimated_delivery_time` | ❌ Нет | Время доставки (минуты) | 25 |
| `priority` | ❌ Нет | Приоритет (чем выше, тем важнее) | 3 |
| `color` | ❌ Нет | Цвет зоны на карте (hex) | "#4CAF50" |
| `buffer_radius_km` | ❌ Нет | Радиус зоны вокруг адресов (км) | 1.5 |

**Как работает `buffer_radius_km`:**
- Скрипт найдет координаты каждого адреса
- Создаст зону вокруг них с радиусом N километров
- Если радиус = 1.5 км, то от каждого адреса в зону войдут все точки в радиусе 1.5 км

### Шаг 2: Запустите импорт

```bash
cd backend
npm run import-zones
```

**Или с указанием файла:**
```bash
npm run import-zones-json -- путь/к/вашему/файлу.json
```

### Шаг 3: Готово! ✅

Скрипт:
1. Прочитает адреса из JSON
2. Найдет их координаты через Google Maps API
3. Создаст зоны доставки с указанным радиусом
4. Сохранит в базу данных

**Пример вывода:**
```
📝 Импорт зон доставки из JSON файла...

✅ Найдено зон: 3

==================================================
📍 Обработка зоны: Центр города
==================================================
   📍 Геокодирование 5 адресов...
      ✅ Knez Mihailova, Belgrade...
      ✅ Trg Republike, Belgrade...
   ✅ Геокодировано: 5/5
   🔄 Создание полигона (радиус буфера: 1.5 км)...
   ✅ Зона создана

==================================================
✅ Импорт завершен!
   Успешно: 3
   Ошибок: 0
==================================================
```

---

## 🗺️ Способ 2: Google My Maps → KML файл

Этот способ позволяет **визуально нарисовать** зоны на карте!

### Шаг 1: Создайте карту в Google My Maps

1. Откройте [Google My Maps](https://www.google.com/maps/d/u/0/)
2. Нажмите **"Создать новую карту"**
3. Дайте карте название (например: "Зоны доставки Hi Food")

### Шаг 2: Нарисуйте зоны доставки

1. Нажмите на иконку **"Draw a line"** (линия с маркером)
2. Выберите **"Add line or shape"** → **"Add polygon"**
3. Кликайте на карте, чтобы нарисовать границы зоны
4. Замкните полигон, кликнув на первую точку

**Совет:** Дайте каждой зоне понятное название (оно будет использоваться для сопоставления с настройками).

![Пример](https://i.imgur.com/example.png)

### Шаг 3: Экспортируйте в KML

1. Нажмите на **три точки** (⋮) рядом с названием карты
2. Выберите **"Export to KML/KMZ"**
3. Снимите галочку **"Export to KMZ instead of KML"**
4. Нажмите **"Download"**

### Шаг 4: Настройте параметры зон

Откройте файл `/backend/scripts/import-zones-from-kml.ts` и отредактируйте `ZONE_CONFIGS`:

```typescript
const ZONE_CONFIGS: Record<string, ZoneConfig> = {
  'Центр города': {  // ← Название должно совпадать с названием в Google My Maps
    name: 'Центр города',
    description: 'Центральная часть Белграда',
    delivery_fee: 200,
    free_delivery_threshold: 2000,
    min_order_amount: 800,
    estimated_delivery_time: 25,
    priority: 3,
    color: '#4CAF50',
  },
  'Нови Београд': {
    name: 'Нови Београд',
    delivery_fee: 300,
    // ...
  },
};
```

### Шаг 5: Запустите импорт

```bash
cd backend
npm run import-zones-kml -- путь/к/файлу.kml
```

**Пример:**
```bash
npm run import-zones-kml -- ~/Downloads/zones.kml
```

### Шаг 6: Готово! ✅

Зоны из KML файла будут импортированы в базу данных.

---

## 🆚 Сравнение методов

| Критерий | JSON с адресами | KML из Google Maps |
|----------|----------------|-------------------|
| **Простота** | ⭐⭐⭐⭐⭐ Очень просто | ⭐⭐⭐⭐ Просто |
| **Точность** | ⭐⭐⭐ Приблизительно | ⭐⭐⭐⭐⭐ Очень точно |
| **Визуализация** | ❌ Нет | ✅ Рисуете на карте |
| **Скорость** | ⭐⭐⭐⭐ Быстро | ⭐⭐⭐⭐⭐ Мгновенно |
| **Гибкость** | ⭐⭐⭐ Круги вокруг адресов | ⭐⭐⭐⭐⭐ Любая форма |

**Рекомендация:**
- 🟢 **Для начала**: используйте JSON с адресами
- 🔵 **Для точной настройки**: переключитесь на KML из Google Maps

---

## 🔄 Обновление зон

Оба метода поддерживают обновление:
- Если зона с таким названием уже существует → она **обновится**
- Если зоны нет → она **создастся**

Вы можете запускать импорт сколько угодно раз!

---

## 🛠️ Настройка перед первым запуском

### 1. Установите зависимости

```bash
cd backend
npm install
```

### 2. Настройте .env

Убедитесь, что у вас есть:

```bash
GOOGLE_MAPS_API_KEY=ваш_ключ_сюда
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=ваш_supabase_ключ
```

### 3. Выполните миграцию БД

В Supabase SQL Editor выполните:
1. `/backend/migrations/001_add_delivery_zones.sql`
2. `/backend/migrations/002_add_zone_management_functions.sql`

---

## 📊 Проверка результатов

После импорта проверьте зоны в базе:

```sql
SELECT name, delivery_fee, is_active, priority
FROM delivery_zones
ORDER BY priority DESC;
```

Или через API:
```bash
curl https://your-backend.com/api/delivery-zones
```

---

## 💡 Советы и рекомендации

### Для JSON с адресами:

**✅ ХОРОШО:**
```json
"addresses": [
  "Knez Mihailova 12, Belgrade, Serbia",
  "Trg Republike, Belgrade",
  "Kalemegdan Park, Belgrade"
]
```

**❌ ПЛОХО:**
```json
"addresses": [
  "улица 1",  // ← Слишком общо
  "дом 5"     // ← Нет города
]
```

**Рекомендации:**
- Указывайте конкретные адреса с номерами домов
- Всегда добавляйте название города
- Используйте английские названия для лучшей точности
- Для больших зон добавляйте 5-10 адресов по периметру
- Радиус буфера: 0.5-2 км (оптимально 1-1.5 км)

### Для KML из Google Maps:

**Советы:**
- Рисуйте плавные контуры без острых углов
- Не делайте зоны слишком маленькими (минимум 500м в диаметре)
- Дайте зонам понятные названия на русском или английском
- Проверяйте, что зоны не пересекаются (или настройте приоритеты)

---

## 🐛 Устранение неполадок

### "Ошибка: GOOGLE_MAPS_API_KEY не установлен"

**Решение:** Добавьте ключ в `/backend/.env`:
```bash
GOOGLE_MAPS_API_KEY=AIzaSy...
```

### "Не удалось геокодировать адрес"

**Причины:**
- Адрес написан неправильно
- Адрес слишком общий
- Превышен лимит Google Maps API

**Решение:**
- Уточните адрес (добавьте номер дома, город)
- Добавьте задержку между запросами (уже есть в скрипте)
- Проверьте квоты в Google Cloud Console

### "В файле не найдено ни одного полигона"

**Для KML:**
- Убедитесь, что вы нарисовали **полигоны**, а не точки или линии
- Экспортируйте именно в **KML** (не KMZ)

### "Нет конфигурации в ZONE_CONFIGS"

**Для KML:**
- Название зоны в Google My Maps должно **точно совпадать** с названием в `ZONE_CONFIGS`
- Проверьте регистр букв и пробелы

---

## 🎯 Примеры использования

### Пример 1: Простая зона доставки

```json
{
  "zones": [
    {
      "name": "Вся Белград",
      "delivery_fee": 300,
      "addresses": [
        "Belgrade City Center",
        "Novi Beograd",
        "Zemun",
        "Voždovac"
      ],
      "buffer_radius_km": 3.0
    }
  ]
}
```

### Пример 2: Несколько зон с разными тарифами

```json
{
  "zones": [
    {
      "name": "Центр - быстрая доставка",
      "delivery_fee": 150,
      "free_delivery_threshold": 1500,
      "estimated_delivery_time": 20,
      "priority": 5,
      "addresses": ["Knez Mihailova, Belgrade"],
      "buffer_radius_km": 1.0
    },
    {
      "name": "Пригород",
      "delivery_fee": 400,
      "free_delivery_threshold": 3000,
      "estimated_delivery_time": 50,
      "priority": 1,
      "addresses": ["Surčin, Belgrade"],
      "buffer_radius_km": 2.5
    }
  ]
}
```

---

## 🚀 Быстрый старт (TL;DR)

**Для JSON:**
```bash
# 1. Отредактируйте адреса
nano backend/config/delivery-zones.json

# 2. Запустите импорт
cd backend
npm run import-zones

# 3. Готово!
```

**Для KML:**
```bash
# 1. Нарисуйте зоны в Google My Maps
# 2. Скачайте KML файл
# 3. Настройте ZONE_CONFIGS в import-zones-from-kml.ts
# 4. Запустите импорт
cd backend
npm run import-zones-kml -- ~/Downloads/zones.kml
```

---

**Готово! 🎉 Теперь у вас есть зоны доставки!**

Вопросы? Смотрите `/DELIVERY_ZONES_SETUP.md` для подробной технической документации.
