# Обновление виджета трекинга заказа

## Что было изменено

### 1. Создан глобальный контекст OrderTrackingContext

**Файл:** `src/context/OrderTrackingContext.tsx`

- Переместили логику трекинга из локального хука в глобальный контекст
- Исправили бесконечный цикл обновления (убрали зависимости orderId и orderStatus из useEffect)
- Добавили автоматическое скрытие виджета через 30 секунд после доставки
- Логика теперь работает на всех страницах приложения

### 2. Переделан компонент OrderStatusTracker

**Файл:** `src/components/OrderStatusTracker.tsx`

**Новый дизайн:**
- Компактная однострочная верстка (как на референсе)
- Fixed позиционирование вверху экрана
- Иконка + текст статуса + номер заказа + кнопка закрытия
- Синхронизирован с кнопками Telegram бота:
  - ✅ "Принят" (accepted)
  - 👨‍🍳 "Готовится" (preparing)
  - 🛵 "В пути" (delivering)
  - 🎉 "Доставлен" (delivered)

**Анимации:**
- `animate-slide-down` - плавное появление сверху
- `animate-bounce-gentle` - легкое покачивание иконки

### 3. Обновлен tailwind.config.js

**Файл:** `tailwind.config.js`

Добавлены анимации:
```js
keyframes: {
  'slide-down': {
    '0%': { transform: 'translateY(-100%)', opacity: '0' },
    '100%': { transform: 'translateY(0)', opacity: '1' },
  },
  'bounce-gentle': {
    '0%, 100%': { transform: 'translateY(0)' },
    '50%': { transform: 'translateY(-5px)' },
  },
}
```

### 4. Интеграция в App.tsx

**Файл:** `src/App.tsx`

- Приложение обернуто в `OrderTrackingProvider`
- Виджет трекинга перенесен в `AppContent` (виден на всех страницах)
- Добавлен padding-top для контента когда виджет активен

### 5. Очистка Home.tsx

**Файл:** `src/pages/Home.tsx`

- Удален импорт старого хука `useOrderTracking`
- Удален компонент виджета из Home (теперь в App.tsx)
- Удален `OrderStatusTracker` из импортов

### 6. Рефакторинг utilities

**Файл:** `src/utils/order.ts` (новый)

Вынесена функция `generateOrderId()` в отдельную утилиту:
```typescript
export const generateOrderId = (): string => {
  return `#${Date.now().toString().slice(-8)}`;
};
```

**Файл:** `src/pages/Cart.tsx`

- Обновлен импорт: `import { generateOrderId } from '@/utils/order';`

### 7. Удален старый хук

**Файл:** `src/hooks/useOrderTracking.ts` - удален

Вся логика перенесена в `OrderTrackingContext.tsx`

---

## Преимущества новой реализации

### ✅ Глобальное состояние
- Виджет виден на всех страницах (Home, Cart, Profile, Favorites)
- Пользователь всегда видит статус заказа при навигации

### ✅ Исправлен бесконечный цикл
- Убрали `orderId` и `orderStatus` из зависимостей useEffect
- Теперь опрос API происходит ровно каждые 10 секунд

### ✅ Компактный дизайн
- Виджет занимает минимум места (одна строка)
- Fixed позиционирование вверху
- Не мешает взаимодействию с контентом

### ✅ Синхронизация с Telegram
- Статусы полностью соответствуют кнопкам Telegram бота
- Иконки и тексты идентичны сообщениям в чате

### ✅ Улучшенный UX
- Плавные анимации появления/исчезновения
- Автоматическое скрытие после доставки (30 сек)
- Кнопка закрытия для ручного скрытия

---

## Как работает виджет

1. **При создании заказа:**
   - Заказ создается в БД со статусом 'accepted'
   - OrderTrackingContext начинает опрос API каждые 10 секунд

2. **Отображение виджета:**
   - Виджет появляется вверху экрана с анимацией slide-down
   - Показывается иконка, текст статуса и номер заказа
   - Виджет виден на всех страницах приложения

3. **Обновление статуса:**
   - Telegram бот обновляет статус заказа в БД (через кнопки)
   - API возвращает обновленный статус
   - Виджет автоматически перерисовывается с новым статусом

4. **Завершение:**
   - При статусе 'delivered' запускается таймер 30 секунд
   - После истечения таймера виджет автоматически скрывается
   - Пользователь может закрыть виджет вручную кнопкой ×

---

## Тестирование

### Сборка проекта
```bash
npm run build
```
✅ Сборка успешна без ошибок

### Dev-сервер
```bash
npm run dev
```
✅ Сервер запущен на http://localhost:5173/hifoodv1/

### Что проверить:

1. **Создание заказа:**
   - Добавьте товары в корзину
   - Оформите заказ
   - Виджет должен появиться вверху экрана со статусом ✅ "Принят"

2. **Навигация между страницами:**
   - Переключайтесь между Home, Cart, Profile, Favorites
   - Виджет должен оставаться виден на всех страницах

3. **Изменение статуса:**
   - В Telegram боте нажмите кнопку 👨‍🍳 "Готовится"
   - Через максимум 10 секунд виджет должен обновиться

4. **Доставка заказа:**
   - Нажмите кнопку 🎉 "Доставлен"
   - Виджет должен автоматически скрыться через 30 секунд

5. **Ручное закрытие:**
   - Нажмите кнопку × на виджете
   - Виджет должен немедленно исчезнуть

---

## Известные ограничения

- Виджет показывает только один активный заказ (последний)
- Опрос API происходит каждые 10 секунд (может быть задержка обновления)
- При закрытии виджета вручную он не появится снова для этого заказа

---

## Файлы изменений

### Созданные файлы:
- `src/context/OrderTrackingContext.tsx`
- `src/utils/order.ts`
- `ORDER_TRACKING_WIDGET_UPDATE.md` (этот документ)

### Измененные файлы:
- `src/components/OrderStatusTracker.tsx`
- `tailwind.config.js`
- `src/App.tsx`
- `src/pages/Home.tsx`
- `src/pages/Cart.tsx`

### Удаленные файлы:
- `src/hooks/useOrderTracking.ts`

---

Дата обновления: 2025-11-04
