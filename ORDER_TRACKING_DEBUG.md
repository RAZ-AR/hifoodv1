# Отладка виджета трекинга заказа

## Проблема: Виджет не показывается

### Возможные причины:

1. **Telegram ID не определяется**
2. **Нет активных заказов в БД**
3. **API не работает или возвращает ошибки**

---

## Шаги отладки

### 1. Проверьте консоль браузера

Откройте DevTools (F12) → Console и найдите логи:

```
📦 OrderTrackingProvider initialized
   Telegram User: {...}
   Telegram ID: 123456789
```

**Если нет Telegram ID:**
- Приложение запущено не через Telegram WebApp
- Нужно тестировать через Telegram или использовать тестовый режим

### 2. Используйте тестовый виджет

В файле `src/App.tsx` уже добавлен тестовый виджет:

```typescript
const ENABLE_TEST_WIDGET = true; // Установите в true
```

**Тестовый виджет появится в правом нижнем углу с кнопками:**
- "Показать/Скрыть виджет"
- "Следующий статус"

Используйте его для проверки внешнего вида и анимаций независимо от API.

### 3. Проверьте API

В консоли браузера должны быть логи:

```
🔄 Fetching active orders for telegram ID: 123456789
📊 Orders from API: 5
✅ Active orders: 1
📋 Latest active order: #89592699 Status: accepted
```

**Если нет логов API:**
- Backend не запущен
- Неверный API_BASE_URL
- CORS ошибка

**Если "Active orders: 0":**
- Все заказы имеют статус 'delivered'
- Создайте новый тестовый заказ

### 4. Проверьте статус заказа в БД

Заказ должен иметь один из статусов:
- `accepted` - Принят ✅
- `preparing` - Готовится 👨‍🍳
- `delivering` - В пути 🛵
- ~~`delivered` - Доставлен~~ (не показывается в виджете)

**Примечание:** Виджет автоматически скрывается для заказов со статусом `delivered`.

---

## Тестирование в Telegram

### Шаг 1: Создайте заказ
1. Откройте бота в Telegram
2. Добавьте товары в корзину
3. Оформите заказ
4. Виджет должен появиться вверху со статусом ✅ "Принят"

### Шаг 2: Измените статус
1. В группе кухни нажмите кнопку 👨‍🍳 "Готовится"
2. Через максимум 10 секунд виджет обновится

### Шаг 3: Проверьте навигацию
1. Переключайтесь между Home, Cart, Profile, Favorites
2. Виджет должен оставаться виден на всех страницах

### Шаг 4: Доставка
1. Нажмите кнопку 🎉 "Доставлен"
2. Виджет исчезнет через 30 секунд

---

## Временное решение: Принудительный показ виджета

Если нужно протестировать виджет без реального заказа, добавьте в `OrderTrackingContext.tsx`:

```typescript
// В начале useEffect добавьте:
if (process.env.NODE_ENV === 'development') {
  // Тестовый заказ
  setOrderId('#TEST12345');
  setOrderStatus('preparing');
  return; // Пропускаем API запросы
}
```

Или используйте тестовый компонент `OrderStatusTrackerTest`.

---

## Проверка компонентов

### OrderStatusTracker.tsx
Компактный виджет должен:
- ✅ Быть fixed top
- ✅ Показывать иконку + статус + номер заказа
- ✅ Иметь кнопку закрытия (×)
- ✅ Анимироваться при появлении (slide-down)

### OrderTrackingContext.tsx
Контекст должен:
- ✅ Опрашивать API каждые 10 секунд
- ✅ Фильтровать только активные заказы (не delivered)
- ✅ Автоматически скрывать после доставки (30 сек)
- ✅ Логировать все действия в консоль

---

## Checklist проверки

- [ ] Dev-сервер запущен (npm run dev)
- [ ] Backend запущен и доступен
- [ ] В консоли видны логи OrderTrackingProvider
- [ ] Telegram ID определяется (или включен тестовый режим)
- [ ] API возвращает заказы
- [ ] Есть хотя бы один активный заказ (не delivered)
- [ ] Виджет появляется вверху экрана
- [ ] Виджет виден на всех страницах
- [ ] Статус обновляется при изменении в Telegram
- [ ] Виджет скрывается после доставки
- [ ] Кнопка × закрывает виджет

---

## Удаление тестового виджета

После проверки удалите из `src/App.tsx`:

```typescript
// Удалите импорт
import OrderStatusTrackerTest from './components/OrderStatusTrackerTest';

// Удалите или установите в false
const ENABLE_TEST_WIDGET = false;

// Удалите рендер
{ENABLE_TEST_WIDGET && <OrderStatusTrackerTest />}
```

И удалите файл:
```bash
rm src/components/OrderStatusTrackerTest.tsx
```

---

## Частые ошибки

### Ошибка: "useOrderTracking must be used within OrderTrackingProvider"
**Решение:** Убедитесь что App обернут в `<OrderTrackingProvider>`

### Виджет не обновляется
**Решение:** Проверьте что заказ в БД обновляется через Telegram бота

### Виджет показывает старый заказ
**Решение:** Очистите localStorage браузера и перезагрузите

### Виджет перекрывает контент
**Решение:** В `App.tsx` добавлен `pt-16` для main когда виджет активен

---

Дата создания: 2025-11-04
