# 📍 Руководство по динамическому отслеживанию заказов

## 🎯 Как работает система

Система автоматически отслеживает статус заказа и обновляет UI в реальном времени.

---

## 📊 Статусы заказа

Заказ проходит через 4 этапа:

1. **`accepted`** ✅ - Заказ принят и передан на кухню
2. **`preparing`** 👨‍🍳 - Готовим ваш заказ
3. **`delivering`** 🛵 - Курьер едет к вам
4. **`delivered`** 🎉 - Заказ доставлен

---

## 🔄 Автоматическое обновление

### Frontend (React)

Компонент `OrderStatusTracker` автоматически:
- Показывается на главной странице при наличии активного заказа
- Обновляет статус каждые **10 секунд** через API
- Скрывается автоматически через **30 секунд** после доставки

**Hook:** `useOrderTracking()`
- Загружает текущий заказ из localStorage
- Периодически опрашивает API (`/api/orders/:orderId/status`)
- Обновляет статус в UI

### Backend (Express + Telegram Bot)

- Создаёт заказ с начальным статусом `accepted`
- Сохраняет заказ в базу данных
- Отправляет заказ в Telegram группу кухни
- Предоставляет кнопки для обновления статуса

---

## 🔧 API Endpoints

### GET `/api/orders/:orderId/status`
Получить текущий статус заказа

**Response:**
```json
{
  "status": "preparing"
}
```

### PATCH `/api/orders/:orderId/status`
Обновить статус заказа (из Telegram кнопок)

**Request:**
```json
{
  "status": "delivering"
}
```

---

## 🤖 Telegram Bot - Кнопки управления

В группе кухни бот отправляет заказ с inline кнопками:

```
✅ Принят | 👨‍🍳 Готовим | 🛵 Везём | 🎉 Доставлен
```

При нажатии на кнопку:
1. Статус обновляется в БД
2. Кнопки обновляются (активная кнопка выделена ✓)
3. Клиент получает уведомление в личку бота
4. Frontend автоматически подхватывает изменение

---

## 💾 База данных

### Таблица `orders`

Статус хранится в колонке `status`:
- Тип: `TEXT`
- Значения: `'accepted' | 'preparing' | 'delivering' | 'delivered'`

---

## 🎨 UI Компонент

### `OrderStatusTracker.tsx`

Визуализирует прогресс заказа:

```tsx
<OrderStatusTracker
  orderId="#87627130"
  status="preparing"
  onClose={() => clearOrder()}
/>
```

**Анимации:**
- Пульсация активного этапа
- Плавный переход прогресс-бара
- Изменение цвета иконок

---

## 📱 Пример работы

### 1. Пользователь оформляет заказ

```typescript
// Frontend: Cart.tsx
const orderId = generateOrderId(); // "#87627130"
saveOrder(orderId, 'accepted');    // Сохраняем в localStorage

await api.sendOrderToTelegram(orderData, customerTelegramId);
```

### 2. Заказ отправляется в Telegram

```typescript
// Backend: telegramBot.ts
await bot.sendMessage(kitchenGroupId, message, {
  reply_markup: {
    inline_keyboard: getStatusButtons(orderId, 'accepted')
  }
});

// Сохраняем в БД
await db.createOrder({
  order_number: orderId,
  status: 'accepted',
  // ... другие поля
});
```

### 3. Повар нажимает кнопку "👨‍🍳 Готовим"

```typescript
// Backend: telegramBot.ts - callback handler
await updateOrderStatus(orderId, 'preparing');
await notifyCustomerAboutStatusChange(orderId, 'preparing');
```

### 4. Frontend автоматически обновляет UI

```typescript
// Frontend: useOrderTracking.ts
setInterval(async () => {
  const status = await api.getOrderStatus(orderId);
  if (status?.status) {
    setOrderStatus(status.status); // 'preparing'
  }
}, 10000); // каждые 10 секунд
```

### 5. Пользователь видит обновление

Трекер заказа обновляется:
- Иконка "Заказ принят" ✅ - завершена (зелёная)
- Иконка "Готовим ваш заказ" 👨‍🍳 - активна (пульсирует)
- Прогресс-бар на 33%

---

## ⚠️ Важные моменты

### 1. **Синхронизация статусов**

Frontend и Backend используют **одинаковые** значения статусов:
- ✅ `accepted`
- ✅ `preparing`
- ✅ `delivering`
- ✅ `delivered`

### 2. **LocalStorage**

Активный заказ хранится в localStorage:
- `currentOrderId` - номер заказа
- `currentOrderStatus` - текущий статус

### 3. **Автоочистка**

Заказ автоматически удаляется из localStorage через 30 секунд после статуса `delivered`.

### 4. **Polling интервал**

По умолчанию 10 секунд. Можно изменить в `useOrderTracking.ts`:

```typescript
const POLL_INTERVAL = 10000; // миллисекунды
```

---

## 🧪 Тестирование

### Ручное тестирование

1. Оформите заказ через Web App
2. Откройте Telegram группу кухни
3. Нажимайте кнопки статусов
4. Наблюдайте обновление на главной странице

### Через консоль браузера

```javascript
// Создать тестовый заказ
localStorage.setItem('currentOrderId', '#12345678');
localStorage.setItem('currentOrderStatus', 'preparing');

// Обновить страницу
location.reload();

// Очистить заказ
localStorage.removeItem('currentOrderId');
localStorage.removeItem('currentOrderStatus');
```

### Через API (curl)

```bash
# Получить статус заказа
curl https://your-backend.com/api/orders/%2387627130/status

# Обновить статус (требуется аутентификация)
curl -X PATCH https://your-backend.com/api/orders/%2387627130/status \
  -H "Content-Type: application/json" \
  -d '{"status":"delivering"}'
```

---

## 🎨 Кастомизация

### Изменить цвета статусов

`OrderStatusTracker.tsx`:

```typescript
const statuses = [
  {
    id: 'accepted',
    label: 'Заказ принят',
    icon: '✅',
    color: 'text-green-500',  // Измените цвет здесь
  },
  // ...
];
```

### Изменить интервал обновления

`useOrderTracking.ts`:

```typescript
const POLL_INTERVAL = 5000; // Проверять каждые 5 секунд
```

### Изменить время автоочистки

`useOrderTracking.ts`:

```typescript
const AUTO_CLEAR_DELAY = 60000; // Удалить через 60 секунд
```

---

## 🐛 Troubleshooting

### Проблема: Статус не обновляется

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что backend запущен
3. Проверьте CORS настройки
4. Убедитесь, что orderId правильный

### Проблема: Заказ не показывается

**Решение:**
1. Проверьте localStorage: `localStorage.getItem('currentOrderId')`
2. Убедитесь, что заказ существует в БД
3. Проверьте формат orderId (должен быть с #)

### Проблема: Кнопки в Telegram не работают

**Решение:**
1. Проверьте, что bot инициализирован
2. Убедитесь, что callback handlers настроены
3. Проверьте логи backend на ошибки

---

## 📚 Связанные файлы

### Frontend:
- `src/components/OrderStatusTracker.tsx` - UI компонент
- `src/hooks/useOrderTracking.ts` - Hook для отслеживания
- `src/services/api.ts` - API методы
- `src/pages/Home.tsx` - Отображение трекера

### Backend:
- `backend/src/index.ts` - API endpoints
- `backend/src/services/telegramBot.ts` - Telegram логика
- `backend/src/services/dataProvider/` - Работа с БД

---

**Автор:** Claude Code
**Дата:** 01.11.2025
**Версия:** 1.0
